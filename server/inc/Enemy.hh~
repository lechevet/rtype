#ifndef		ENEMY_HH
# define	ENEMY_HH

# include	"AEntity.hh"

class		Enemy : public AEntity
{
public:
  Enemy();
    ~Enemy();
  int		getLife() const;
  void		getDamage(int power, EntityType type);
  bool		move();
};

extern "C"
{
  Enemy*	Create();
    void	Destroy(Enemy *);
}

#endif
